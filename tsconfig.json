{
  "compilerOptions": {
    "target": "ES6", // 목표 JavaScript 버전 (ES5, ES6, 등)
    "module": "CommonJS", // 모듈 시스템 (CommonJS, ES6, 등)
    "strict": true, // 엄격한 타입 검사 활성화
    "esModuleInterop": true, // CommonJS 모듈을 ES 모듈처럼 사용할 수 있도록 함
    "skipLibCheck": true, // 라이브러리 검사 생략
    "forceConsistentCasingInFileNames": true, // 파일 이름의 대소문자 일관성 검사
    "outDir": "./dist", // 컴파일된 파일을 저장할 디렉토리 경로
    "baseUrl": "./", // 상대 모듈 경로의 기준 디렉토리
    "paths": {
      "*": ["./src/*"] // 모든 모듈을 src 디렉토리에서 찾음
    },
    "sourceMap": true, // 소스 맵 생성
    "declaration": true, // .d.ts 타입 선언 파일 생성
    "downlevelIteration": true // ES5 환경에서 for...of 루프 지원
  },
  "include": [
    "src/**/*.ts" // 컴파일할 파일 패턴
  ],
  "exclude": [
    "node_modules" // 제외할 디렉토리 패턴
  ]
}
//with gpt setting